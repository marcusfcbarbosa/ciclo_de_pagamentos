import React from 'react'

// Dados extraidos do template LteAdmin
export default props => (
    <section className='content-header'>
        <h1> { props.tittle } <small> {props.subtittle} </small> </h1>       
    </section>
)